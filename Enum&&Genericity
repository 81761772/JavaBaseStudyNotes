1.当定义一个枚举类型时，每个枚举类型成员都可以看作是枚举类型的一个实例（一般类的实例都需要new来产生），这些枚举成员都默认被final,public,static
  修饰，所以当需要使枚举类型成员时直接使用枚举类型名称调用枚举类型成员即可。
  
2.在枚举类型中可以添加构造方法，但是规定这个构造方法必须被private修饰。

3.一些集合框架已经被泛型化了。

4.泛型的类型参数只能是对象类型，不能是基本类型（如int)，如需在泛型中操作基本类型，可使用封装类（如使用Integer对int进行封装）。
