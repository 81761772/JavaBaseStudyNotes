 1.使用声明src的方式在html文件中调用js文件中的函数时，在html中需要两段<script>，一段用来指定src，另一段用来调用函数。

2.include标记指令和include动作标识的区别：
  ①标记指令通过file属性指定被包含的文件，且file属性不支持任何表达式；动作标识通过page属性指定被包含的文件，且page属性
   支持JSP表达式。
  ②使用标记指令时，被包含的文件内容会原封不动地插入到包含页中，最终被JSP编译器编译成一个Java文件；而是用动作标识时，
   程序会将请求转发到被包含的页面，并将执行结果输出到浏览器中，然后返回包含页继续执行后面的代码。因为服务器执行的是
   多个文件，故JSP编译器会分别对这些文件进行编译。
  ③标记指令的被包含文件中不能有重名的变量或方法，而动作标识可以有。
